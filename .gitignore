# Django configurations
db.sqlite3
/backend/db.sqlite3

# Environment files
.env
/backend/.env

# Python caching
__pycache__/
*.pyc
*.pyo
*.pyd

# ML model files & outputs
*.pt
annotated_synthetic_output.png
synthetic_block.png

# Media & Temp Files
/backend/media/
/backend/error_500_traceback.html
scratch/

# Android Build Artifacts
**/android/.gradle/
**/android/build/
**/android/app/build/
**/modules/granite-ar/android/build/
*.apk


# Python virtual environment for the backend (1.2 GB).
# Previously only self-ignored by the venv's own auto-created .gitignore stub;
# removing that stub would have pulled the whole tree into the index.
/backend/venv/

# macOS finder metadata.
.DS_Store

# Raw capture footage used for AR bench testing (75 MB). Kept locally; git is
# the wrong place for large binary video and it is not needed to build or run
# any part of the system.
/Test-images/*.MP4
/Test-images/*.mov
